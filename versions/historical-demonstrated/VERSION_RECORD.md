# Candidate Screening — Historical Demonstrated Generation

**Status:** HISTORICAL EXECUTION EVIDENCE  
**Current authority:** NO

## What survives

A historical walkthrough/demo recorded a screening flow operating as a working system at that time. That evidence is preserved because it demonstrates that a functioning generation existed, but it must not be used to claim the current checked-in JSON still behaves the same way.

## Historical system intent

The screening system was designed to accept candidate/CV information, extract structured content, compare against job requirements, detect duplicates, perform structured AI-assisted evaluation, persist candidate state and return a screening response.

## Why this generation is separated

The current checked-in 13-node export was later inspected and found to contain defects that conflict with a simple “working demo” description. Therefore historical execution proof and current implementation truth must remain separate.

## Evidence boundary

Supported:
- a historical demonstrated screening flow existed;
- historical walkthrough evidence may be referenced as historical.

Not supported:
- that the current JSON is identical to the demonstrated generation;
- that current duplicate detection/state handling works;
- that the historical demo proves current buyer readiness.

## Media policy

Historical generation: preserve links/evidence if available, but do **not** create new empty demo/screenshot placeholders here.