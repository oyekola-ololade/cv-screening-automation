# Candidate Screening — Current Demo Placeholder

**Status:** NO CURRENT VERIFIED END-TO-END DEMO.

The historical walkthrough belongs to an earlier generation and must not be reused as proof that the current checked-in export works.

## Replacement gate

A current demo should be recorded only after the repair is implemented and should show:

- representative candidate intake;
- valid extraction path;
- low-confidence/failed extraction path;
- authoritative duplicate lookup with duplicate and non-duplicate cases;
- preserved candidate state through AI evaluation;
- schema-validated structured evaluation;
- human-review path where required;
- final persistence/response;
- one failure/recovery case;
- exact workflow version/date/environment.

Until then, this placeholder is intentionally visible.