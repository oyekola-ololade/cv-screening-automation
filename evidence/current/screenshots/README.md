# Candidate Screening — Current Screenshot Placeholder

**Status:** CURRENT RUNTIME SCREENSHOT SET MISSING.

The repository already includes `assets/current-vs-target.svg`, which is a diagnostic/architecture visual. It is **not** a current successful execution screenshot.

## Required current screenshots after repair

- full repaired workflow overview;
- valid extraction execution;
- failed/low-confidence extraction execution;
- authoritative duplicate lookup: duplicate case;
- authoritative duplicate lookup: new candidate case;
- AI structured-output/schema validation;
- human-review decision path;
- final persistence/update result;
- one controlled error/failure path.

Each image should name the workflow version and test date. Historical walkthrough frames should remain historical.